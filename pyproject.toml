[tool.poetry]
name = "pylox"
version = "0.1.0"
description = "A Tree-walk interpreter for the Lox programming language"
authors = ["Harold Karibiye <haroldkaribiye@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
